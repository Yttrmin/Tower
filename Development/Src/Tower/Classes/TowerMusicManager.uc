class TowerMusicManager extends Info
	config(Tower);

var TowerPlayerController PlayerOwner;	/** Owner of this MusicManager */
var globalconfig float MusicVolume; /** Maximum volume for music audiocomponents (max value for VolumeMultiplier). */